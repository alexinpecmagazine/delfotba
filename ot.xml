var id = new URLSearchParams(location.search).get("id") || "alcazar_arquitecnico";

fetch("https://alexinpecmagazine.github.io/delfotba/" + id + ".html")
  .then(e => e.text())
  .then(e => {
    document.open();
    document.write(e);
    document.close();
  });
